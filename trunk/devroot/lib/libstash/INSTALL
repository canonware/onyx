#-*-mode:text-*-
#############################################################################
#
# $Source$
# $Author: jasone $
# $Revision: 139 $
# $Date: 1998-07-10 13:45:38 -0700 (Fri, 10 Jul 1998) $
#
# <<< Description >>>
#
# Building and installation instructions for libstash.
#
#############################################################################

Building libstash in many cases is as simple as typing the following while in
the root directory of the source tree:

    ./configure
    make

To install, do the above, then type:

   make install

Optionally, pass any of the following arguments to 'configure':

--prefix=<install-root-dir>

	Set the base directory in which to install.  For example:
	
	./configure --prefix=/usr/local

	will install SQRL into /usr/local/lib, /usr/local/include/libstash,
	and /usr/local/man/.

--disable-shared

	Build only static libraries.  Do not build shared libraries.